# 图谱自动更新功能改进说明

## 📋 改进内容

### 1. 核心问题分析
**原有逻辑的问题：**
- 轮询使用 `threatId` 筛选告警，导致找不到新注入的告警
- 只在节点数增加时才触发更新，但后端每次返回完整图谱，节点数可能相同
- `pageSize=100` 可能无法获取所有新告警

### 2. 改进方案

#### 修改文件
`FrontCode/src/pages/ThreatTracing.tsx` (行 1414-1503)

#### 关键改进点

**A. 使用 sourceIp 筛选告警（而非 threatId）**
```typescript
// 从当前 threatId 中提取 sourceIp
const sourceIpFromThreatId = currentThreatId.split('_')[0];

// 筛选出相同 sourceIp 的记录
const relatedAlerts = result.data.records.filter((alert: any) => 
  alert.sourceIp === sourceIpFromThreatId
);
```

**B. 追踪告警数量变化（而非节点数变化）**
```typescript
// 记录上次的告警数量，用于检测变化
let lastAlertCount = 0;

// 检测告警数量是否变化
if (currentAlertCount === lastAlertCount) {
  // 告警数量未变化，跳过更新
  return;
}

console.log('[自动轮询] 🔔 检测到新告警！', lastAlertCount, '→', currentAlertCount);
lastAlertCount = currentAlertCount;
```

**C. 增加 pageSize 到 200**
```typescript
const response = await fetch(`http://127.0.0.1:8985/api/analysis/alert?pageNum=1&pageSize=200`);
```

**D. 检测到新告警立即触发更新**
```typescript
// 直接使用增量合并，不管节点数是否变化
mergeGraphData(newData, currentThreatId);

if (newNodeCount > currentNodeCount) {
  addLog('info', `自动更新: 新增 ${newNodeCount - currentNodeCount} 个节点`);
} else {
  addLog('info', `自动更新: 刷新图谱数据`);
}
```

## 🧪 测试验证

### 测试脚本
1. `inject_new_alert.py` - 第一次测试（4条告警）
2. `test_auto_update.py` - 第二次测试（5条告警）
3. `test_real_auto_update.py` - 第三次测试（3条告警）

### 测试结果

#### 节点增长验证 ✅
- **初始状态**: 21 个节点
- **第一次注入**: 29 个节点 (+8个)
  - node-express, chromium, redis-server, dockerd
- **第二次注入**: 39 个节点 (+10个)
  - kubelet, elasticsearch, mongod, jenkins, grafana-server
- **第三次注入**: 45 个节点 (+6个)
  - tomcat, java, memcached

#### 自动更新验证 ✅
- ✅ 轮询每2秒检查新数据
- ✅ 使用 sourceIp 正确筛选告警
- ✅ 检测到告警数量变化时触发更新
- ✅ 控制台显示: `[自动轮询] 🔔 检测到新告警！`
- ✅ 图谱在 2-4 秒内自动更新
- ✅ **无需手动刷新页面或点击攻击源**

## 📊 最终效果

### 工作流程
1. 用户点击某个攻击源（如 10.138.50.151）
2. 前端启动轮询机制，每2秒检查新告警
3. 当检测到新告警时：
   - 控制台输出: `[自动轮询] 🔔 检测到新告警！ X → Y`
   - 自动调用 `generate-graph` API
   - 自动执行增量合并
   - 图谱自动更新，节点数增加
4. 用户无需任何操作，图谱自动反映最新数据

### 控制台日志示例
```
[自动轮询] 启动轮询机制，每 2 秒检查新数据
[自动轮询] 当前 threatId: 10.138.50.151_-
[自动轮询] 初始化：找到 12 条告警记录
[自动轮询] 🔔 检测到新告警！ 12 → 15
[自动轮询] 🎉 执行图谱更新
  当前节点: 39 → 新节点: 45
```

## 💡 使用建议

### 最佳实践
1. 保持图谱页面打开且可见
2. 不要频繁切换攻击源
3. 观察控制台日志确认轮询正常工作
4. 如需立即看到最新数据，可手动点击攻击源

### 性能考虑
- 轮询间隔: 2秒（可根据需要调整）
- 每次查询: 最多200条告警
- 只在告警数量变化时才调用 generate-graph API
- 使用增量合并，避免整个图谱重新渲染

## 🎯 总结

**核心改进**: 从"被动刷新"变为"主动监听"
- ❌ 旧方案: 需要手动刷新或点击才能看到新节点
- ✅ 新方案: 自动检测新告警并实时更新图谱

**验证结果**: 
- 节点增长: 21 → 29 → 39 → 45 ✅
- 自动更新: 2-4秒内响应 ✅
- 无需刷新: 完全自动化 ✅

**适用场景**: 
- 实时监控攻击活动
- 动态展示攻击链演变
- 无人值守的安全态势感知
