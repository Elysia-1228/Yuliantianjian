# 御链天鉴 - 项目代码结构完整文档

> **文档版本**: v1.0  
> **生成日期**: 2025-01-19  
> **项目名称**: Zhilian2025 网络安全态势感知与主动防御平台

---

## 📋 目录

1. [项目整体架构](#1-项目整体架构)
2. [后端代码结构](#2-后端代码结构)
3. [前端代码结构](#3-前端代码结构)
4. [功能模块详解](#4-功能模块详解)
5. [数据库设计](#5-数据库设计)
6. [API接口规范](#6-api接口规范)
7. [部署与配置](#7-部署与配置)

---

## 1. 项目整体架构

### 1.1 项目根目录结构

```
Zhilian2025-master（胡景山版）/
├── BackCode/                    # 后端Spring Boot项目
├── FrontCode/                   # 前端React项目
├── PythonIDS/                   # Python AI检测引擎
├── RuleBasedIDS/                # 基于规则的IDS
├── scripts/                     # 运维脚本
│   ├── host_monitor_agent.py   # 主机监控Agent
│   └── ...
├── docs/                        # 项目文档
├── test/                        # 测试报告
├── net_safe-v2.sql             # 数据库初始化脚本
├── package.json                 # 项目启动配置
├── start-all.bat               # Windows一键启动脚本
├── start-all.ps1               # PowerShell启动脚本
└── README.md                    # 项目说明文档
```

### 1.2 技术栈总览

| 层级 | 技术栈 | 版本 | 说明 |
|------|--------|------|------|
| **后端框架** | Spring Boot | 3.5.7 | 核心业务逻辑 |
| **持久层** | MyBatis | 3.5.11 | ORM框架 |
| **数据库** | MySQL | 8.0+ | 关系型数据库 |
| **缓存** | Redis | - | 指令队列缓存 |
| **连接池** | Druid | 1.2.20 | 数据库连接池 |
| **前端框架** | React | 18.2.0 | UI框架 |
| **构建工具** | Vite | 5.1.4 | 前端构建工具 |
| **UI样式** | TailwindCSS | 3.4.1 | CSS框架 |
| **图表库** | Recharts/ECharts | - | 数据可视化 |
| **AI引擎** | Python + PyTorch | 3.8+ | 威胁检测 |
| **认证** | JWT | 0.11.5 | Token认证 |
| **WebSocket** | Spring WebSocket | - | 实时通信 |

### 1.3 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        用户层 (User Layer)                    │
│                  安全分析师 / 系统管理员                        │
└────────────────────────┬────────────────────────────────────┘
                         │ HTTPS
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                    前端展示层 (Frontend)                       │
│   React + Vite + TailwindCSS + Recharts                      │
│   - 实时监控大屏  - 威胁告警面板  - 报告生成                   │
└────────────────────────┬────────────────────────────────────┘
                         │ REST API / WebSocket
                         ▼
┌─────────────────────────────────────────────────────────────┐
│                  业务逻辑层 (Backend Service)                  │
│   Spring Boot + MyBatis + JWT                                │
│   - 认证授权  - 威胁分析  - 主机监控  - 报告生成               │
└────────┬───────────────┬───────────────┬────────────────────┘
         │               │               │
         ▼               ▼               ▼
┌────────────┐  ┌────────────┐  ┌────────────────┐
│   MySQL    │  │   Redis    │  │  AI Report     │
│  业务数据   │  │  指令队列   │  │  GenAI引擎     │
└────────────┘  └────────────┘  └────────────────┘
         │
         ▼
┌─────────────────────────────────────────────────────────────┐
│                  感知与响应层 (Detection & Response)           │
│   - Python TransecGAN (AI检测)                               │
│   - Snort (规则检测)                                          │
│   - HIDS Agent (主机监控 + 防火墙控制)                         │
└─────────────────────────────────────────────────────────────┘
```

---

## 2. 后端代码结构

### 2.1 后端目录树

```
BackCode/
├── pom.xml                              # Maven依赖配置
├── src/main/
│   ├── java/com/yukasl/backcode/
│   │   ├── BackCodeApplication.java    # 主启动类
│   │   ├── config/                      # 配置类
│   │   │   ├── CorsConfig.java         # 跨域配置
│   │   │   ├── JwtProperties.java      # JWT配置
│   │   │   ├── RedisConfig.java        # Redis配置
│   │   │   ├── WebMvcConfig.java       # MVC配置
│   │   │   └── WebSocketConfig.java    # WebSocket配置
│   │   ├── controller/                  # 控制器层
│   │   │   ├── AiReportController.java           # AI报告生成
│   │   │   ├── AnalysisController.java           # 威胁分析
│   │   │   ├── AuthController.java               # 认证登录
│   │   │   ├── CollectionHostController.java     # 数据采集配置
│   │   │   ├── DashBoardController.java          # 仪表盘
│   │   │   ├── MonitorController.java            # 主机监控
│   │   │   ├── ReportConfigController.java       # 报告配置
│   │   │   ├── ReportShareController.java        # 报告共享
│   │   │   ├── ThreatController.java             # 威胁响应
│   │   │   ├── TracingController.java            # 攻击溯源
│   │   │   └── UploadController.java             # 文件上传
│   │   ├── service/                     # 业务逻辑层
│   │   │   ├── AuthService.java                  # 认证服务接口
│   │   │   ├── MonitorService.java               # 监控服务接口
│   │   │   ├── ThreatService.java                # 威胁服务接口
│   │   │   └── Impl/                             # 服务实现类
│   │   │       ├── AuthServiceImpl.java
│   │   │       ├── MonitorServiceImpl.java
│   │   │       ├── ThreatServiceImpl.java
│   │   │       └── ...
│   │   ├── mapper/                      # MyBatis Mapper接口
│   │   │   ├── AuthMapper.java
│   │   │   ├── MonitorMapper.java
│   │   │   ├── ThreatMapper.java
│   │   │   └── ...
│   │   ├── pojo/                        # 数据对象
│   │   │   ├── entity/                           # 实体类
│   │   │   │   ├── sysUser.java                 # 系统用户
│   │   │   │   ├── hostStatusMonitor.java       # 主机状态监控
│   │   │   │   ├── processMonitor.java          # 进程监控
│   │   │   │   ├── potentialThreatAlert.java    # 威胁告警
│   │   │   │   ├── threatSourceTracing.java     # 威胁溯源
│   │   │   │   ├── networkThreatCollectionHost.java  # 采集主机
│   │   │   │   ├── networkThreatCollectionApi.java   # 采集API
│   │   │   │   ├── threatReportConfig.java      # 报告配置
│   │   │   │   ├── reportShare.java             # 报告共享
│   │   │   │   ├── ThreatReportHistory.java     # 报告历史
│   │   │   │   └── orgInfo.java                 # 组织信息
│   │   │   ├── DTO/                              # 数据传输对象
│   │   │   │   ├── HostMonitorDTO.java
│   │   │   │   ├── ProcessMonitorDTO.java
│   │   │   │   ├── alertPageDTO.java
│   │   │   │   ├── collectionHostDTO.java
│   │   │   │   ├── reportConfigDTO.java
│   │   │   │   └── ...
│   │   │   └── VO/                               # 视图对象
│   │   │       ├── DashBoardVO.java
│   │   │       ├── sysUserVO.java
│   │   │       ├── FileUploadVO.java
│   │   │       └── ...
│   │   ├── result/                      # 统一响应结果
│   │   │   ├── Result.java                       # 统一返回封装
│   │   │   └── PageResult.java                   # 分页结果
│   │   ├── exception/                   # 异常处理
│   │   │   └── GlobalExceptionHandler.java
│   │   ├── interceptor/                 # 拦截器
│   │   │   └── JwtTokenInterceptor.java          # JWT拦截器
│   │   ├── utils/                       # 工具类
│   │   │   ├── JwtUtil.java                      # JWT工具
│   │   │   ├── Md5Util.java                      # MD5工具
│   │   │   └── CommandQueueService.java          # 指令队列服务
│   │   └── websocket/                   # WebSocket
│   │       └── ThreatWebSocketHandler.java
│   └── resources/
│       ├── application.yml              # 主配置文件
│       ├── application-dev.yml          # 开发环境配置
│       └── mapper/                      # MyBatis XML映射
│           ├── AuthMapper.xml
│           ├── MonitorMapper.xml
│           ├── ThreatMapper.xml
│           └── ...
└── target/                              # 编译输出目录
```

### 2.2 核心配置文件

#### application.yml
```yaml
server:
  port: 8985                    # 后端服务端口

spring:
  profiles:
    active: dev                 # 激活开发环境配置
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
  data:
    redis:
      host: localhost
      port: 6379

mybatis:
  mapper-locations: classpath:mapper/*.xml
  type-aliases-package: com.yukasl.backcode.pojo.entity
  configuration:
    map-underscore-to-camel-case: true

jwt:
  secret-key: your-secret-key
  ttl: 7200000                  # 2小时过期
```

### 2.3 核心类说明

#### 2.3.1 控制器层 (Controller)

| 控制器 | 路径前缀 | 主要功能 |
|--------|---------|---------|
| **AuthController** | `/api/auth` | 用户登录认证，JWT Token颁发 |
| **MonitorController** | `/api/host/monitor` | 主机状态监控数据查询与上报 |
| **ThreatController** | `/api/threats` | 威胁告警处理、IP封禁/解封 |
| **AnalysisController** | `/api/analysis` | 威胁分析、流量统计、趋势图表 |
| **AiReportController** | `/api/report` | AI报告生成、历史管理 |
| **DashBoardController** | `/api/dashboard` | 仪表盘聚合数据 |
| **TracingController** | `/api/tracing` | 攻击溯源结果查询 |
| **CollectionHostController** | `/api/collection/host` | 数据采集主机配置 |
| **ReportShareController** | `/api/report/share` | 报告共享管理 |
| **UploadController** | `/api/upload` | 威胁情报文件上传 |

#### 2.3.2 服务层 (Service)

**核心服务接口**：
- `AuthService`: 用户认证、密码校验
- `MonitorService`: 主机监控数据CRUD、状态更新
- `ThreatService`: 威胁告警处理、封禁指令下发
- `CommandQueueService`: Redis指令队列管理

**服务实现特点**：
- 使用 `@Service` 注解标注
- 通过 `@Autowired` 注入Mapper
- 使用 `PageHelper` 实现分页
- 异常统一抛出由全局异常处理器捕获

#### 2.3.3 数据访问层 (Mapper)

**MyBatis Mapper接口**：
- 使用 `@Mapper` 注解
- XML文件定义SQL语句
- 支持动态SQL、结果映射

**示例 - MonitorMapper.java**：
```java
@Mapper
public interface MonitorMapper {
    List<hostStatusMonitor> queryHostMonitor(HostMonitorDTO dto);
    hostStatusMonitor queryHostMonitorByHostId(String hostId);
    void insertHostStatus(hostStatusMonitor status);
    void updateHostStatus(hostStatusMonitor status);
}
```

#### 2.3.4 实体类 (Entity)

**核心实体**：

1. **hostStatusMonitor** - 主机状态监控
   - `hostId`: 主机IP
   - `cpuUsage`: CPU使用率
   - `memoryUsage`: 内存使用率
   - `diskUsage`: 磁盘使用率
   - `networkConn`: 网络连接数
   - `fileStatus`: 文件状态(JSON)
   - `cpuModel`: CPU型号
   - `memoryInfo`: 内存信息
   - `diskPartitions`: 磁盘分区(JSON)

2. **potentialThreatAlert** - 威胁告警
   - `threatId`: 威胁ID
   - `sourceIp`: 攻击源IP
   - `targetIp`: 目标IP
   - `attackType`: 攻击类型
   - `severity`: 严重程度
   - `status`: 处理状态
   - `detectedTime`: 检测时间

3. **sysUser** - 系统用户
   - `username`: 用户名
   - `password`: 密码(MD5)
   - `role`: 角色
   - `status`: 状态

---

## 3. 前端代码结构

### 3.1 前端目录树

```
FrontCode/
├── package.json                 # 依赖配置
├── vite.config.ts              # Vite构建配置
├── tailwind.config.js          # TailwindCSS配置
├── index.html                  # HTML入口
├── public/                     # 静态资源
│   └── maps/                   # 地图数据
│       └── china.json
├── src/
│   ├── main.tsx                # React入口
│   ├── App.tsx                 # 根组件
│   ├── index.css               # 全局样式
│   ├── pages/                  # 页面组件
│   │   ├── Login.tsx                    # 登录页
│   │   ├── Home.tsx                     # 首页仪表盘
│   │   ├── HostMonitoring.tsx           # 主机安全监控
│   │   ├── ProcessMonitoring.tsx        # 进程监控
│   │   ├── ThreatAlerts.tsx             # 威胁告警
│   │   ├── ThreatAnalysis.tsx           # 威胁分析
│   │   ├── ThreatTracing.tsx            # 攻击溯源
│   │   ├── DataCollection.tsx           # 数据采集配置
│   │   ├── ReportGeneration.tsx         # 报告生成
│   │   └── OrgManagement.tsx            # 组织管理
│   ├── components/             # 公共组件
│   │   ├── Sidebar.tsx                  # 侧边栏导航
│   │   ├── Header.tsx                   # 顶部导航
│   │   └── ...
│   ├── services/               # API服务
│   │   └── connector.ts                 # 后端接口封装
│   ├── types/                  # TypeScript类型定义
│   │   └── index.ts
│   └── utils/                  # 工具函数
│       └── auth.ts                      # 认证工具
└── dist/                       # 构建输出
```

### 3.2 核心页面组件

#### 3.2.1 Login.tsx - 登录页
**功能**：
- 用户名密码登录
- JWT Token获取与存储
- 登录状态验证
- 自动跳转首页

**关键代码**：
```typescript
const handleLogin = async () => {
  const response = await fetch('/api/auth/login', {
    method: 'POST',
    body: JSON.stringify({ username, password })
  });
  const data = await response.json();
  localStorage.setItem('token', data.data.token);
  navigate('/');
};
```

#### 3.2.2 Home.tsx - 首页仪表盘
**功能**：
- 实时威胁统计卡片
- 攻击趋势图表
- 最新告警列表
- 系统健康度展示

**数据源**：
- `/api/dashboard/summary` - 聚合摘要
- `/api/analysis/trend?range=24h` - 趋势数据
- WebSocket实时推送

#### 3.2.3 HostMonitoring.tsx - 主机安全监控
**功能**：
- CPU/内存/磁盘实时监控
- 磁盘分区详细展示
- 网络连接数图表
- 文件完整性监控

**特色**：
- 3D可视化磁盘卡片
- 动态颜色告警(>90%红色)
- 圆形进度指示器
- 扫描线动画效果

**数据格式**：
```typescript
interface HostStatus {
  hostId: string;
  cpuUsage: number;
  cpuModel: string;
  memoryUsage: number;
  memoryInfo: string;
  diskUsage: number;
  diskPartitions: Array<{
    name: string;
    percent: number;
    used: number;
    free: number;
    total: number;
  }>;
  fileStatus: Array<{
    path: string;
    status: 'normal' | 'missing' | 'protected';
  }>;
}
```

#### 3.2.4 ThreatAlerts.tsx - 威胁告警
**功能**：
- 威胁告警列表展示
- 严重程度筛选
- 一键封禁/解封IP
- 告警详情查看

**操作流程**：
1. 点击"封禁"按钮
2. 调用 `/api/threats/{id}/block`
3. 后端下发指令到Agent
4. 前端更新状态为"已封禁"

#### 3.2.5 ReportGeneration.tsx - AI报告生成
**功能**：
- 选择报告类型(日报/周报)
- AI自动生成报告
- 历史报告管理
- Markdown预览
- PDF导出

**生成流程**：
```typescript
const generateReport = async (type: string) => {
  const response = await fetch('/api/report/generate', {
    method: 'POST',
    body: JSON.stringify({ type })
  });
  const report = await response.json();
  setCurrentReport(report.data);
};
```

### 3.3 服务层封装 (connector.ts)

**核心方法**：
```typescript
// 获取主机监控数据
export const getHostStatus = async (hostId: string) => {
  return await apiClient.get(`/api/host/monitor/realtime/${hostId}`);
};

// 封禁IP
export const blockThreat = async (threatId: number) => {
  return await apiClient.post(`/api/threats/${threatId}/block`);
};

// 生成报告
export const generateReport = async (type: string) => {
  return await apiClient.post('/api/report/generate', { type });
};
```

**统一错误处理**：
```typescript
apiClient.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

---

## 4. 功能模块详解

### 4.1 认证授权模块

**技术实现**：JWT Token

**流程**：
1. 用户输入用户名密码
2. 后端验证 → 生成JWT Token
3. 前端存储Token到localStorage
4. 后续请求携带Token在Header中
5. 后端拦截器验证Token有效性

**关键代码**：
```java
// JwtUtil.java
public static String createJWT(String secretKey, long ttl, Map<String, Object> claims) {
    return Jwts.builder()
        .setClaims(claims)
        .setExpiration(new Date(System.currentTimeMillis() + ttl))
        .signWith(SignatureAlgorithm.HS256, secretKey)
        .compact();
}
```

### 4.2 主机监控模块

**架构**：Agent → Backend → Frontend

**Agent功能** (`host_monitor_agent.py`):
- 使用 `psutil` 采集系统指标
- 每3秒上报一次数据
- 支持Windows/Linux双平台
- 获取CPU型号、内存类型、磁盘分区

**数据流**：
```
Agent采集 → POST /api/host/monitor/report → 
MySQL存储 → GET /api/host/monitor/realtime/{hostId} → 
前端展示
```

**核心指标**：
- CPU使用率、型号、核心数、频率
- 内存使用率、类型、总容量
- 磁盘使用率、分区详情
- 网络连接数
- 文件完整性状态

### 4.3 威胁检测与响应模块

**检测引擎**：
1. **TransecGAN AI引擎** (Python)
   - 基于Transformer的GAN模型
   - 异常流量检测
   - 零日漏洞识别

2. **Snort规则引擎**
   - 基于签名的检测
   - 已知攻击模式匹配

**响应流程**：
```
检测引擎发现威胁 → 
POST /api/analysis/alert (上报告警) → 
安全分析师确认 → 
POST /api/threats/{id}/block (下发封禁指令) → 
指令进入Redis队列 → 
Agent轮询获取指令 → 
执行防火墙规则 → 
IP被封禁
```

**封禁实现**：
- **Windows**: `netsh advfirewall firewall add rule`
- **Linux**: `iptables -I INPUT -s {IP} -j DROP`

### 4.4 AI报告生成模块

**技术栈**：GenAI (Google Gemini)

**生成流程**：
1. 前端选择报告类型
2. 后端查询威胁数据
3. 调用GenAI API生成Markdown
4. 保存到数据库
5. 返回前端渲染

**报告内容**：
- 威胁态势总结
- 攻击类型分布
- 高危事件详情
- 防御建议
- 趋势预测

**导出功能**：
- Markdown实时预览
- html2canvas截图
- jsPDF生成PDF

### 4.5 数据采集配置模块

**功能**：
- 配置外部主机监控
- 配置API数据源
- 定时采集任务

**应用场景**：
- 监控云上主机
- 接入第三方威胁情报
- 多数据源聚合

### 4.6 攻击溯源模块

**功能**：
- 攻击链路还原
- IP地理位置定位
- 攻击时间线展示
- 3D地图可视化

**数据来源**：
- 威胁告警表
- 网络流量日志
- 主机监控日志

---

## 5. 数据库设计

### 5.1 核心表结构

#### sys_user - 系统用户表
```sql
CREATE TABLE sys_user (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) UNIQUE NOT NULL,
    password VARCHAR(100) NOT NULL,
    role VARCHAR(20) DEFAULT 'admin',
    status TINYINT DEFAULT 1,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### host_status_monitor - 主机状态监控表
```sql
CREATE TABLE host_status_monitor (
    id INT PRIMARY KEY AUTO_INCREMENT,
    host_id VARCHAR(50) NOT NULL,
    cpu_usage DOUBLE,
    cpu_model VARCHAR(200),
    cpu_cores INT,
    cpu_freq DOUBLE,
    memory_usage DOUBLE,
    memory_info VARCHAR(100),
    memory_total_gb DOUBLE,
    memory_used_gb DOUBLE,
    network_conn INT,
    disk_usage DOUBLE,
    disk_info VARCHAR(100),
    disk_total_gb DOUBLE,
    disk_used_gb DOUBLE,
    disk_free_gb DOUBLE,
    disk_partitions TEXT,
    file_status TEXT,
    monitor_time DATETIME,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_host_id (host_id),
    INDEX idx_monitor_time (monitor_time)
);
```

#### potential_threat_alert - 威胁告警表
```sql
CREATE TABLE potential_threat_alert (
    id INT PRIMARY KEY AUTO_INCREMENT,
    threat_id VARCHAR(50) UNIQUE,
    source_ip VARCHAR(50),
    target_ip VARCHAR(50),
    attack_type VARCHAR(50),
    severity VARCHAR(20),
    confidence DOUBLE,
    payload TEXT,
    status VARCHAR(20) DEFAULT 'pending',
    detected_time DATETIME,
    handled_time DATETIME,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP,
    INDEX idx_source_ip (source_ip),
    INDEX idx_status (status),
    INDEX idx_detected_time (detected_time)
);
```

#### threat_report_history - 报告历史表
```sql
CREATE TABLE threat_report_history (
    id INT PRIMARY KEY AUTO_INCREMENT,
    title VARCHAR(200),
    content LONGTEXT,
    report_type VARCHAR(50),
    generated_time DATETIME,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

### 5.2 表关系说明

```
sys_user (1) -------- (N) threat_report_history
                            (报告创建者)

host_status_monitor (N) ---- (1) 主机IP
                                  (同一主机多条记录)

potential_threat_alert (N) -- (1) source_ip
                                  (同一IP多次攻击)
```

---

## 6. API接口规范

### 6.1 统一响应格式

```json
{
  "code": 200,
  "msg": "success",
  "data": { ... }
}
```

**状态码**：
- `200`: 成功
- `400`: 参数错误
- `401`: 未授权
- `500`: 服务器错误

### 6.2 分页参数

```json
{
  "pageNum": 1,
  "pageSize": 10,
  "total": 100,
  "data": [...]
}
```

### 6.3 核心接口列表

详见 [README.md 第4章 API接口文档](#)

---

## 7. 部署与配置

### 7.1 环境要求

- **JDK**: 17+
- **Maven**: 3.6+
- **Node.js**: 16+
- **MySQL**: 8.0+
- **Redis**: 6.0+
- **Python**: 3.8+

### 7.2 快速启动

#### 方式一：一键启动(推荐)
```bash
# Windows
start-all.bat

# Linux/Mac
npm start
```

#### 方式二：分别启动
```bash
# 后端
cd BackCode
mvn spring-boot:run

# 前端
cd FrontCode
npm run dev

# Agent
cd scripts
python host_monitor_agent.py
```

### 7.3 配置文件说明

#### application-dev.yml
```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/net_safe
    username: root
    password: your_password
```

#### vite.config.ts
```typescript
export default defineConfig({
  server: {
    port: 3000,
    proxy: {
      '/api': {
        target: 'http://localhost:8985',
        changeOrigin: true
      }
    }
  }
});
```

### 7.4 数据库初始化

```bash
mysql -uroot -p < net_safe-v2.sql
```

---

## 8. 项目特色功能

### 8.1 实时监控大屏
- 玻璃拟态设计风格
- 动态数据刷新
- WebSocket实时推送
- 响应式布局

### 8.2 智能威胁检测
- AI深度学习模型
- 零日漏洞识别
- 低误报率
- 自适应白名单

### 8.3 主动防御
- 毫秒级响应
- 自动封禁IP
- 防火墙联动
- 误封保护机制

### 8.4 可视化报告
- AI自动生成
- Markdown渲染
- PDF导出
- 历史归档

---

## 9. 开发规范

### 9.1 代码风格
- 后端：遵循阿里巴巴Java开发手册
- 前端：使用ESLint + Prettier
- 命名：驼峰命名法

### 9.2 Git提交规范
```
feat: 新功能
fix: 修复bug
docs: 文档更新
style: 代码格式调整
refactor: 重构
test: 测试
chore: 构建/工具变动
```

### 9.3 注释规范
- 类/方法必须有注释
- 复杂逻辑添加行内注释
- API接口注释包含参数说明

---

## 10. 常见问题

### Q1: 前端无法连接后端？
**A**: 检查后端是否启动，端口是否为8985，CORS配置是否正确。

### Q2: Agent上报数据失败？
**A**: 检查后端地址配置，确认网络连通性，查看Agent日志。

### Q3: 数据库连接失败？
**A**: 检查MySQL服务状态，用户名密码是否正确，数据库是否创建。

### Q4: Maven依赖下载慢？
**A**: 配置国内镜像源(阿里云)。

---

## 附录

### A. 项目依赖清单

#### 后端依赖
- spring-boot-starter-web
- mybatis-spring-boot-starter
- mysql-connector-j
- druid-spring-boot-starter
- spring-boot-starter-data-redis
- jjwt (JWT认证)
- hutool-all (工具类)
- pagehelper (分页)

#### 前端依赖
- react + react-dom
- react-router-dom (路由)
- axios (HTTP客户端)
- recharts (图表)
- lucide-react (图标)
- tailwindcss (样式)
- html2canvas + jspdf (PDF导出)

#### Python依赖
- psutil (系统监控)
- requests (HTTP请求)

---

**文档维护**: Zhilian Security Team  
**最后更新**: 2025-01-19  
**版权所有**: © 2025 Zhilian Security Platform
