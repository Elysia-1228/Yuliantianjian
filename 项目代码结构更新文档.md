# 御链天鉴 - 项目代码结构更新文档 v2.0

> **生成日期**: 2025-01-26  
> **更新说明**: 对比v1.0文档，重点标注新增功能与架构变化

---

## 🆕 主要更新内容概览

### 关键变化
1. **新增区块链后端模块** (`backend/`) - Hyperledger Fabric集成
2. **AI引擎远程集成** - PIDS溯源图谱生成（10.138.50.151:5000）
3. **图谱自动更新机制** - 实时监控攻击链演变（2秒轮询）
4. **新增实体类** - `networkThreatUpload`, `threatTrafficStat`, `userinfo`
5. **前端组件增强** - `PIDSGraph`, `GalaxyTopology`, `MacroView`, `RadarLoading`

---

## 1. 项目整体架构

### 1.1 项目根目录结构变化

```
Zhilian2025-master（胡景山版）/
├── BackCode/                    # 🔵 主后端Spring Boot项目（原有）
├── backend/                     # 🆕 区块链后端（新增）
├── FrontCode/                   # 前端React项目
├── PythonIDS/                   # Python AI检测引擎
├── RuleBasedIDS/                # 基于规则的IDS
├── scripts/                     # 运维脚本
├── docs/                        # 项目文档
├── test/                        # 测试报告
├── dist/                        # 🆕 构建输出目录（新增）
├── net_safe-v2.sql             # 数据库初始化脚本
├── 自动更新改进说明.md          # 🆕 图谱自动更新文档（新增）
└── README.md
```

### 1.2 技术栈新增项

| 技术栈 | 版本 | 说明 | 状态 |
|--------|------|------|------|
| **区块链后端** | Spring Boot 2.7.12 | Fabric Gateway集成 | 🆕 新增 |
| **区块链框架** | Hyperledger Fabric 2.2.5 | 数据上链与查询 | 🆕 新增 |
| **图谱渲染** | AntV G6 | PIDS溯源图谱 | 🆕 新增 |
| **远程AI引擎** | Flask | PIDS图谱生成 | 🆕 新增 |

---

## 2. 后端代码结构

### 2.1 BackCode 主要变化

#### 新增控制器
- **TracingController** - 🔄 增强：新增AI图谱生成接口

#### 新增服务
- **TracingService** - 🆕 攻击溯源、AI引擎调用、降级处理

#### 新增实体类（3个）
1. **networkThreatUpload** - 威胁情报上传
2. **threatTrafficStat** - 流量统计
3. **userinfo** - 用户信息

#### 新增DTO
- **tracingPageDTO** - 溯源分页DTO
- **pageTrafficDateDTO** - 流量日期分页DTO

#### 新增Mapper
- **TracingMapper** - 溯源数据访问层

#### 核心配置新增
```yaml
# application.yml
external:
  ai-engine:
    url: ${AI_ENGINE_URL:http://10.138.50.151:5000/predict}
```

### 2.2 🆕 backend 区块链后端（新增模块）

```
backend/
├── pom.xml                              # Spring Boot 2.7.12
├── README.md                            # 区块链后端说明
├── AI_ENGINE_INTEGRATION.md             # 🆕 AI引擎集成文档
├── src/main/
│   ├── java/com/example/blockchain/
│   │   ├── Application.java            # 主启动类
│   │   ├── BlockchainController.java   # 区块链控制器
│   │   ├── FabricConfig.java           # Fabric配置
│   │   ├── service/AsyncService.java   # 异步批处理服务
│   │   ├── dto/                         # 5个DTO类
│   │   │   ├── OrgInfoDTO.java
│   │   │   ├── ThreatAlertDTO.java
│   │   │   ├── TrafficStatDTO.java
│   │   │   ├── ReportConfigDTO.java
│   │   │   └── SourceTracingDTO.java
│   │   └── util/AESUtil.java           # AES加密工具
│   └── resources/
│       ├── application.yml
│       └── static/index.html           # 控制台页面
```

**核心功能**：
- 异步批量上链（队列聚合 + 熔断重试）
- 富查询（按类型查询）
- WebSocket实时推送
- API Key鉴权
- 选择性字段加密（AES）

---

## 3. 前端代码结构

### 3.1 新增组件（4个）

```
FrontCode/src/components/
├── PIDSGraph/                   # 🆕 PIDS溯源图谱组件
│   ├── index.tsx               # 图谱主组件（AntV G6）
│   └── formatter.ts            # 数据格式化工具
├── GalaxyTopology/              # 🆕 银河拓扑图组件
│   └── index.tsx
├── MacroView/                   # 🆕 宏观视图组件
│   └── index.tsx
└── RadarLoading/                # 🆕 雷达加载动画
    ├── index.tsx
    └── styles.css
```

### 3.2 🆕 ThreatTracing.tsx 重大更新

**新增功能**：
1. **PIDS溯源图谱可视化**
   - 使用AntV G6渲染攻击链路
   - 支持节点拖拽、缩放、平移
   - 动态节点样式（正常/受损/关键）

2. **自动更新机制**（核心改进）
   ```typescript
   // 每2秒轮询检查新告警
   const pollInterval = setInterval(async () => {
     // 使用 sourceIp 筛选告警（而非 threatId）
     const relatedAlerts = result.data.records.filter(
       (alert: any) => alert.sourceIp === sourceIpFromThreatId
     );
     
     // 检测告警数量变化时自动触发更新
     if (currentAlertCount !== lastAlertCount) {
       console.log('[自动轮询] 🔔 检测到新告警！');
       mergeGraphData(newData.data, currentThreatId);
     }
   }, 2000);
   ```

3. **AI引擎集成**
   - 调用 `/api/tracing/result/generate-graph`
   - 自动降级到本地规则（AI引擎离线时）
   - 3秒超时保护

**测试验证**：
- ✅ 节点增长: 21 → 29 → 39 → 45
- ✅ 自动更新: 2-4秒内响应
- ✅ 无需刷新: 完全自动化

---

## 4. 新增功能模块

### 🆕 4.1 区块链证据存证模块（backend）

**技术**: Hyperledger Fabric Gateway

**功能**：
- 异步批量上链（聚合入队数据）
- 富查询（按类型查询: ORG/ALERT/TRAFFIC/REPORT/TRACE）
- WebSocket实时推送结果
- API Key鉴权 + AES加密

**数据流**：
```
业务数据 → POST /api/chain/{type} → 入队 → 
异步批处理 → Fabric链码 → 上链成功 → 
WebSocket推送 → 前端通知
```

### 🆕 4.2 AI引擎远程集成模块

**架构**: 前端 → BackCode → 远程AI引擎（10.138.50.151:5000）

**功能**：
1. **PIDS溯源图谱生成**
   - 输入: `source_ip`, `target_ip`, `attack_type`
   - 输出: 图谱节点和边数据

2. **降级处理**
   - AI引擎离线时自动切换到本地规则
   - 基于攻击类型生成默认图谱
   - 3秒超时保护

**核心实现**（TracingServiceImpl.java）：
```java
public JsonNode generateTraceGraph(String sourceIp, String targetIp, String attackType) {
    try {
        // 尝试调用远程AI引擎
        return callRemoteAiEngine(sourceIp, targetIp, attackType);
    } catch (Exception e) {
        // 降级处理
        log.warn("AI Engine Offline, using fallback mode");
        return generateFallbackGraph(sourceIp, targetIp, attackType);
    }
}
```

### 🆕 4.3 图谱自动更新机制

**核心改进**（详见`自动更新改进说明.md`）：
1. 使用 `sourceIp` 筛选告警（而非 `threatId`）
2. 追踪告警数量变化（而非节点数变化）
3. 增量合并图谱数据（避免重新渲染）
4. 轮询间隔: 2秒，查询范围: 200条

---

## 5. 数据库设计

### 🆕 5.1 新增表结构（3个）

#### network_threat_upload - 威胁情报上传表
```sql
CREATE TABLE network_threat_upload (
    id INT PRIMARY KEY AUTO_INCREMENT,
    file_name VARCHAR(200) NOT NULL,
    file_size BIGINT,
    file_path VARCHAR(500),
    upload_time DATETIME,
    status VARCHAR(20) DEFAULT 'pending',
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### threat_traffic_stat - 流量统计表
```sql
CREATE TABLE threat_traffic_stat (
    id INT PRIMARY KEY AUTO_INCREMENT,
    stat_time DATETIME NOT NULL,
    total_packets BIGINT,
    total_bytes BIGINT,
    threat_count INT,
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

#### userinfo - 用户信息表
```sql
CREATE TABLE userinfo (
    id INT PRIMARY KEY AUTO_INCREMENT,
    username VARCHAR(50) NOT NULL,
    email VARCHAR(100),
    phone VARCHAR(20),
    create_time DATETIME DEFAULT CURRENT_TIMESTAMP
);
```

---

## 6. API接口规范

### 🆕 6.1 新增接口

#### 攻击溯源接口（BackCode）
```
POST /api/tracing/result/generate-graph
请求: { "source_ip", "target_ip", "attack_type" }
响应: { "nodes": [...], "edges": [...], "metadata": {...} }
```

#### 区块链接口（backend）
```
POST /api/chain/org          # 组织信息上链
POST /api/chain/alert        # 威胁告警上链
POST /api/chain/traffic      # 流量统计上链
POST /api/chain/report       # 报告配置上链
POST /api/chain/trace        # 溯源数据上链
GET  /api/chain/list/{type}  # 按类型查询
GET  /api/evidence/{id}      # 证据查询
POST /api/verify             # 哈希验证
```

---

## 7. 部署与配置

### 🆕 7.1 新增环境要求

| 组件 | 版本 | 说明 |
|------|------|------|
| **Docker** | 20.10+ | Fabric网络（backend需要） |
| **WSL2** | - | Fabric部署环境（backend需要） |

### 🆕 7.2 Fabric网络部署（backend模块）

```bash
# 1. 在WSL中部署Fabric网络
git clone https://github.com/hyperledger/fabric-samples ~/hyperledger/fabric-samples
cd ~/hyperledger/fabric-samples/test-network
./network.sh up createChannel -c mychannel -ca
./network.sh deployCC -c mychannel -ccn evidence -ccl java -ccp <链码路径>

# 2. 在WSL中启动backend
cd backend
mvn spring-boot:run
```

### 7.3 快速启动（更新）

```bash
# 1. 启动BackCode后端
cd BackCode
mvn spring-boot:run          # 端口: 8985

# 2. 启动backend区块链后端（可选）
cd backend
mvn spring-boot:run          # 端口: 8080

# 3. 启动前端
cd FrontCode
npm run dev                  # 端口: 3000

# 4. 启动Agent
cd scripts
python host_monitor_agent.py

# 5. 启动远程AI引擎（可选）
# 在远程服务器10.138.50.151上
python app.py                # 端口: 5000
```

---

## 8. 常见问题

### 🆕 Q1: AI引擎连接超时？
**A**: 
- 检查AI引擎服务: `curl http://10.138.50.151:5000/health`
- 检查网络连通性: `ping 10.138.50.151`
- 系统会自动降级到本地规则生成图谱

### 🆕 Q2: 区块链后端连接失败？
**A**: 
- 确认在WSL中部署并启动Fabric网络
- 检查 `connection-org1.yaml` 路径
- 验证证书和私钥文件存在
- 查看Docker容器状态: `docker ps`

### 🆕 Q3: 图谱自动更新不生效？
**A**: 
- 检查控制台是否有 `[自动轮询]` 日志
- 确认已点击某个攻击源
- 检查告警数量是否变化

---

## 9. 更新日志

### v2.0 (2025-01-26)
- 🆕 新增区块链后端模块（backend/）
- 🆕 集成Hyperledger Fabric Gateway
- 🆕 新增AI引擎远程集成（10.138.50.151:5000）
- 🆕 实现PIDS溯源图谱自动更新机制
- 🆕 新增3个实体类和4个前端组件
- 🆕 增强TracingController和TracingService
- 🆕 新增AI_ENGINE_INTEGRATION.md文档
- 🆕 新增自动更新改进说明.md文档

### v1.0 (2025-01-19)
- ✅ 初始版本发布

---

## 📊 项目统计对比

| 指标 | v1.0 | v2.0 | 变化 |
|------|------|------|------|
| **后端模块** | 1 | 2 | +1 (backend) |
| **控制器** | 11 | 13 | +2 |
| **实体类** | 11 | 14 | +3 |
| **前端组件** | 5 | 9 | +4 |
| **数据库表** | 12 | 15 | +3 |

---

**文档维护**: Zhilian Security Team  
**最后更新**: 2025-01-26  
**版权所有**: © 2025 Zhilian Security Platform
